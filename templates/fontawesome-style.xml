<?xml version="1.0" encoding="utf-8"?>
<resources>

<% _.each(glyphs, function(glyph) { %><string name="<%= glyph.name %>">&#x<%= glyph.codepoint.toString(16).toUpperCase() %>;</string>
<% }); %>

</resources>